const arr = Array(5).fill().map((value, index) => {
    return index * 2 + 3;
});

console.log(arr);

const arr2 = Array(5).fill().map((value, index) => {
    return index * 2 + 3;
})

console.log(arr2);